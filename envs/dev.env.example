## Setup

1. Copy the `dev.env.example` file to `dev.env` in the `envs` folder.
2. Add your OpenAI API key and other environment variables to `dev.env`:

   ```bash
   OPENAI_API_KEY=your_openai_api_key_here